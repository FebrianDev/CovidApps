<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CovidApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/ic_baseline_keyboard_arrow_down_24</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!--    My Custom Color-->
    <style name="TextPrimary" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white_primary</item>
    </style>

    <color name="colorPrimary">@color/white_primary</color>
    <color name="colorSecondary">@color/white_secondary</color>
    <color name="colorThird">@color/white_third</color>

    <color name="bgColorPrimary">@color/black_primary</color>
    <color name="bgColorSecondary">@color/black_secondary</color>
    <color name="bgColorThird">@color/black_third</color>

    <color name="bgBottomColor">@color/black</color>

</resources>